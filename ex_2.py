data = input("What is the input string? ") # Get the input from the user for the string to count the number of characters and store it as data
count = len(data) # calculte the length of data and store it as count
print(f"{data} has {count} character") # dispay the output